/** SVG donut: r=80, circumference = 2πr */
const DONUT_R = 80
export const DONUT_C = 2 * Math.PI * DONUT_R
export const DONUT_L1 = (1 / 3) * DONUT_C
export const DONUT_L2 = (1 / 3) * DONUT_C
export const DONUT_L3 = (1 / 3) * DONUT_C
